apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rclone.fullname" . }}-test-connection"
  labels:
    {{- include "rclone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rclone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
